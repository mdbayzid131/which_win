import 'package:flutter/widgets.dart';

class WhichWin extends StatelessWidget {
  const WhichWin({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
